{
  "name": "nyc-transit-nav",
  "version": "1.0.0",
  "private": true,
  "description": "Local web app for getting around NYC: Google Maps directions/isochrones + live MTA subway arrival times.",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "gtfs-realtime-bindings": "^2.1.0"
  }
}
